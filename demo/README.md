# Demo

```bash
source .venv/bin/activate
python -m demo
# http://localhost:8088
```

Views with `@Menu` appear in the sidebar navigation. Views without it (like `dialog_demo` and `greet`) are still routable but hidden from the menu.
